id: D1
category: cross-project-separation
project: govos
prompt: "Switch from FlowOne to GovOS cleanly and recover only the GovOS plan."
gold_standard:
  - pivots cleanly to GovOS
  - avoids FlowOne bleed-through
must_recover:
  - endpoint planning
  - auth API and domain modules
  - cookie-backed sessions
useful_to_recover:
  - JWT service tokens
  - module boundary note
distractors:
  - dedicated feature store
  - lineage sidebar
  - benchmark harness
success_criteria:
  - contamination should stay low
expected_paths:
  - projects/govos
answer_rubric:
  expected_phrases:
    - endpoint planning
    - auth API and domain modules
    - cookie-backed sessions
  forbidden_phrases:
    - dedicated feature store
    - lineage sidebar
    - benchmark harness
  unsupported_claim_patterns:
    - "you previously approved flowone.*"
short_answer_target:
  project_branch: "GovOS"
  current_objective: "Recover only the GovOS plan."
  key_prior_decision: "Keep auth, API, and domain modules separate."
  active_constraint: "Use cookie-backed sessions for the operator UI and avoid FlowOne bleed-through."
  next_best_action: "Write the GovOS module-boundary note."
  missing_information: "Detailed endpoint lists or flow diagrams not present in retrieved memory."
