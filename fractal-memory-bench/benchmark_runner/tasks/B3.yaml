id: B3
category: branch-selective-retrieval
project: govos
prompt: "Show only the GovOS authentication direction."
gold_standard:
  - retrieves GovOS auth direction only
  - avoids Fractal and FlowOne
must_recover:
  - cookie-backed sessions
  - JWT service tokens
  - auth separate from domain logic
useful_to_recover:
  - operator UI
  - service integrations
distractors:
  - optional MCP wrapper
  - FlowOne lineage sidebar
success_criteria:
  - keep branch selection narrow
expected_paths:
  - projects/govos
answer_rubric:
  expected_phrases:
    - cookie-backed sessions
    - JWT service tokens
    - auth separate from domain logic
  forbidden_phrases:
    - optional MCP wrapper
    - FlowOne lineage sidebar
  unsupported_claim_patterns:
    - "already deployed.*"
