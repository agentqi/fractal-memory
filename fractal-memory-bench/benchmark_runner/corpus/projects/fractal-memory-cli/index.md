# Fractal Memory CLI

Fractal Memory CLI is the CLI-first MVP for a local-first structured memory system.

- Build for standard execution first.
- Keep it JIT-first and AOT-ready later.
- MCP is an optional wrapper, not the design center.
