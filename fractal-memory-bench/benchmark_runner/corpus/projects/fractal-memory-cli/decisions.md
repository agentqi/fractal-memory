# Decisions

- Keep all core logic independent of CLI and MCP.
- Treat MCP as an optional interoperability layer.
- Benchmark harness should be Python-first, deterministic, and export CSV, JSON, and Markdown.
