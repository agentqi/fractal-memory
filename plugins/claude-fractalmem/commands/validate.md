---
description: Validate the FractalMem repository structure and metadata
allowed-tools:
  - mcp__fractalmem__memory_validate
---

Validate the FractalMem repository.

Call `memory_validate`. Group errors and warnings by node path and propose concrete fixes for each issue. Surface any stale-index warning so the user can run `/memory refresh`.
