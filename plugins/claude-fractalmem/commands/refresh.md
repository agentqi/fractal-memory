---
description: Rebuild FractalMem aliases, tags, and paths indexes
allowed-tools:
  - mcp__fractalmem__memory_index_refresh
---

Refresh the FractalMem indexes.

Call `memory_index_refresh` to rebuild aliases, tags, paths, and the per-node cache from filesystem truth. Confirm completion to the user.
