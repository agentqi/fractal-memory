# Benchmarks

The benchmark harness lives in a separate repository:

https://github.com/agentqi/fractal-memory-bench

Keeping benchmarks separate keeps this product repository small and focused on the CLI, core library, MCP server, and agent plugins.

The benchmark repository contains the Python runner, corpus, task definitions, adapters, and optional MemoryBench bridge used to compare structured memory approaches.
